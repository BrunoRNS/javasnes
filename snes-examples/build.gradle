plugins {
    id 'java'
    id 'application'
}

repositories {
    flatDir {
        dirs 'lib'
    }
}

dependencies {
    implementation name: 'javasnes-1.0.0'
}

if (!hasProperty('application')) {

    mainClassName = 'Main'

} else {

    application {
        mainClass = 'Main'
    }

}
