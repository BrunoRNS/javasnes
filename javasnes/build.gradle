plugins {
    id 'java'
}

group = 'org.javasnes'
version = '1.0.0'

sourceCompatibility = '1.8'
targetCompatibility = '1.8'

task copyJarToExamples(type: Copy) {
    dependsOn jar
    from jar.archivePath
    into "$rootDir/snes-examples/lib"
}

build.finalizedBy(copyJarToExamples)
